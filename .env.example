# MongoDB Connection - Choose one option:

# Option 1: Local MongoDB (if you install MongoDB locally)
# MONGODB_URI=mongodb://localhost:27017/cute-couple-app

# Option 2: MongoDB Atlas (Free Cloud Database - Recommended)
# Sign up at https://www.mongodb.com/atlas and get your connection string
# Replace <username>, <password>, and <cluster> with your details
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/cute-couple-app?retryWrites=true&w=majority

# Server Configuration
PORT=5000
JWT_SECRET=your-super-secret-jwt-key-here-make-it-long-and-random

# Production Settings (for deployment)
NODE_ENV=development
